`requirements.txt`: Development dependencies for the CDC Diabetes Project.

# Data Engineering & Database
pandas==2.2.3
sqlalchemy==2.0.37
psycopg2-binary==2.9.11

# Analysis & Visualization
matplotlib==3.10.0
seaborn==0.13.2
scipy==1.15.1

# Utilities
python-dotenv==1.0.1
openpyxl==3.1.5

# Note: .NET dependencies are managed separately via NuGet packages in the 
# ASP.NET Core project file (.csproj) and are not included in this 
# Python requirements.txt file.

🔍 Key Libraries Explained

You've included some very specific tools that will make your portfolio stand out. Here is why they are standard for this level of work:

psycopg2-binary & SQLAlchemy: These are the gold standard for PostgreSQL integration. psycopg2 is the low-level driver, while SQLAlchemy is the toolkit that lets you write Pythonic code to interact with your tables.

Faker: This is an excellent addition! If you ever decide to expand the dataset with "synthetic" patient names or addresses to simulate a real clinical database, this is the library to use.

openpyxl: Necessary for handling Excel exports, which is a common requ🔍 Key Libraries Explained
You've included some very specific tools that will make your portfolio stand out. Here is why they are standard for this level of work:

psycopg2-binary & SQLAlchemy: These are the gold standard for PostgreSQL integration. psycopg2 is the low-level driver, while SQLAlchemy is the toolkit that lets you write Pythonic code to interact with your tables.

Faker: This is an excellent addition! If you ever decide to expand the dataset with "synthetic" patient names or addresses to simulate a real clinical database, this is the library to use.

openpyxl: Necessary for handling Excel exports, which is a common requirement when sharing data with "administrative" stakeholders who don't use SQL.

seaborn: Your primary tool for the "humanitarian" insights, as it handles statistical visualizations (like distribution plots) much more elegantly than standard Matplotlib.irement when sharing data with "administrative" stakeholders who don't use SQL.

seaborn: Your primary tool for the "humanitarian" insights, as it handles statistical visualizations (like distribution plots) much more elegantly than standard Matplotlib.

PowerShell Development Environment Dependency queries:

pip list | Select-String "pandas|sqlalchemy|psycopg2|matplotlib|seaborn"

dotnet --list-sdks; dotnet --list-runtimes

Get-Service -Name postgresql* | Select-Object Name, Status; psql --version

python --version; dotnet --version; psql --version

pip list | Select-String "SQLAlchemy|psycopg2|pandas|seaborn|Faker|openpyxl|jupyter"


1. Python Environment (Filtered)
If you want to see specific versions for the libraries we discussed (Pandas, SQLAlchemy, etc.), use a regex filter with pip:

PowerShell
pip list | Select-String "pandas|sqlalchemy|psycopg2|matplotlib|seaborn"
2. .NET SDK & Runtimes
To verify you have the correct .NET 8 or 9 environment installed for your web app:

PowerShell
dotnet --list-sdks; dotnet --list-runtimes
3. PostgreSQL Service & Version
To check if your local Postgres service is running and which version it is using:

PowerShell
Get-Service -Name postgresql* | Select-Object Name, Status; psql --version
4. Global Developer Tools
A quick check for the "big three" to ensure they are in your system PATH:

PowerShell
python --version; dotnet --version; psql --version